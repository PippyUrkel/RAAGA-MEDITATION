{
	"name": "raga-meditation-player",
	"version": "0.0.0",
	"description": "",
    "scripts": {
        "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && python3 backend/flask_api.py",
        "dev": "concurrently \"npm run dev --prefix frontend\" \"python3 backend/flask_api.py\""
    },

	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^9.1.2"
	}
}
